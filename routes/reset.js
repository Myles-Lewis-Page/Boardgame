// Disabled: the full-library reset tool has been removed. This file is kept
// as an inert stub (not mounted in server.js) rather than deleted, in case
// it's ever needed again.
const express = require('express');
const router = express.Router();
module.exports = router;
